## Considerations

- **Rate limiting based on a given flag**  
  _e.g._ `nerv --limit=10`  
  _(Limits output; not sorted in any particular order.)_

- **Sorting via flags**  
  _e.g._ `nerv --sort=asc` or `nerv --sort=desc`  
  _(Currently with respect to memory.)_

- **Real-time updates**

- **Better memory formatting**

- **Specific process information**  
  _e.g._ `nerv --show [PID]` 
