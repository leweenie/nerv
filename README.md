### Considerations

- Rate limiting based on a given flag  
  _e.g._ `nerv --limit=10` _(limits output tonot sorted in any particular order)._

- Sorting via flags  
  _e.g._ `nerv --sort=asc` or `nerv --sort=desc` _(currently wrt. memory)._
